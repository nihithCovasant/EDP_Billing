[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cams-otel-lib"
version = "0.1.2"
description = "Local dev stub for cams-otel-lib"

[tool.setuptools.packages.find]
where = ["."]
include = ["cams_otel_lib*"]
